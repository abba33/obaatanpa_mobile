{
  "build": {
    "env": {
      "FLUTTER_WEB": "true"
    }
  },
  "installCommand": "curl -fsSL https://storage.googleapis.com/flutter_infra_release/releases/stable/linux/flutter_linux_3.24.0-stable.tar.xz | tar -xJ && export PATH=\"$PWD/flutter/bin:$PATH\" && flutter pub get",
  "buildCommand": "export PATH=\"$PWD/flutter/bin:$PATH\" && flutter build web",
  "outputDirectory": "build/web"
}